%YAML 1.2
---

board_a:
  dictionary: DICT_4X4_50
  squares_x: 7
  squares_y: 5
  square_length: 0.0395   # meters (39.5mm)
  marker_length: 0.030    # meters (30mm)

board_b:
  dictionary: DICT_4X4_50
  squares_x: 7
  squares_y: 5
  square_length: 0.0395
  marker_length: 0.030

calibration:
  min_samples: 15
  min_translation_m: 0.05    # minimum rig translation between poses (meters)
  min_rotation_deg: 15.0     # minimum rig rotation between poses (degrees)
  capture_cooldown_s: 2.0    # seconds to wait between captures (let rig settle)
  camera1_image_topic: /camera1/color/image_raw
  camera1_info_topic:  /camera1/color/camera_info
  camera2_image_topic: /camera2/color/image_raw
  camera2_info_topic:  /camera2/color/camera_info
  output_file: /tmp/calibration_result.yaml
